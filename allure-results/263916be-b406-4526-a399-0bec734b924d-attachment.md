# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: LoginPatient.spec.ts >> Verify the Login As Patient
- Location: tests\LoginPatient.spec.ts:18:5

# Error details

```
ReferenceError: page is not defined
```

# Test source

```ts
  1  | import { test ,expect } from "@playwright/test";
  2  | 
  3  | let config = {
  4  |     appUrl:"https://sniffleshealth.devtrust.biz/login",
  5  |     email:"test@59gmail.com",
  6  |     password:"Test@1234"
  7  | };
  8  | 
  9  | //This Hooks Before Each Test
  10 | test.beforeEach(async({page})=>{
  11 |     await page.goto(config.appUrl);
  12 | })
  13 | 
  14 | // Optional cleanup after each test
  15 | test.afterEach(async ({ page }) => {
  16 |   await page.close(); // Close browser tab (good practice in local/dev run)
  17 | });
  18 | test("Verify the Login As Patient",async()=>{
  19 | 
  20 |     //Enter valid credentials and log in
> 21 |     await page.locator('input[name="email"]').fill(config.email);
     |     ^ ReferenceError: page is not defined
  22 |     await page.locator('input[name="password"]').fill(config.password);
  23 |     await page.getByRole("button", { name: "Log in" }).click();
  24 | })
  25 | 
```